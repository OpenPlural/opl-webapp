# Open Plural (Webapp)

## Requirements:
- `npm` Package Manager
- `ng` Angular CLI

## Running the app:
1. Make sure you have the backend running locally
2. Run the app using `ng serve --ssl` in the project directory.
3. You can now access the app at `https://localhost:4200/`
